impl Solution {
    pub fn running_sum(nums: Vec<i32>) -> Vec<i32> {
        let mut sum: i32 = 0;

        nums.iter().map( |num| {
            sum += num
            sum
        }).collect()
    }
}